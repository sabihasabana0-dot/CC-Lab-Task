float findAverage(int arr[], int size) {
    int sum = 0;
    for(int i = 0; i < size; i++) {
        sum += arr[i];
    }
    return (float)sum / size;
}

int main() {
    int marks[] = {75, 80, 92, 68, 87};
    float avg = findAverage(marks, 5);
    printf("Average = %.2f", avg);
    return 0;
}
